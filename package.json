{
  "name": "best-ip-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "uv run python scripts/dev.py",
    "dev:no-reload": "uv run python scripts/dev.py --no-reload",
    "check": "node --check worker/index.js && node --check frontend/app.js && node --check frontend/action-client.js && node --check frontend/zip-reader.js && node --check scripts/decrypt_subscription.mjs",
    "test:frontend": "node --test tests/frontend_modules.test.mjs tests/frontend_app.test.mjs",
    "test:worker": "node --test tests/worker/*.test.mjs",
    "test": "npm run check && npm run test:frontend && npm run test:worker",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "jose": "^6.2.10"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
